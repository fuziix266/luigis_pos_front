name: luigis_pos
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.0.0
  go_router: ^12.0.0
  flutter_bloc: ^8.1.0
  equatable: ^2.0.5
  shared_preferences: ^2.2.0
  audioplayers: ^5.0.0
  url_launcher: ^6.1.0
  intl: ^0.18.0
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/sounds/
